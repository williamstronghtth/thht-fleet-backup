# 2026-04-09 Daily Log

## Platform Migration: OpenClaw → Claude Code
- Now running on Claude Code, not OpenClaw
- Old OpenClaw-specific references (openclaw.json, `openclaw agents bind`, config keys) are stale
- New tools available: CronCreate/CronList/CronDelete, WebSearch/WebFetch, scheduling skill
- Inter-agent inbox system: ~/agents/<agent-id>/workspace/inbox/
- Need to confirm with William what's migrated vs. what needs setup
