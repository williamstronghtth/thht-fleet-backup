
## CRM Auth Migration - Real Progress
- **Committed & pushed** team auth system to thht-crm main branch
  - `services/auth.js` — session-based cookie auth
  - `public/login.html` — clean login page
  - `server.js` — auth middleware, login/logout endpoints, /api/me
  - `.env.example` — CRM_USERS format documented
- **Tested locally** — full flow works:
  - GET /login → 200 (login page)
  - GET / (unauth) → 302 redirect to /login
  - POST /login → session cookie set
  - GET / (authed) → 200 (CRM app)
  - GET /api/clients (authed) → 200
  - GET /api/clients (unauth) → 401
- **Repo cleanup** — removed crm.db-shm/wal, updated .gitignore
- **Both commits pushed** to GitHub main
- **Render** hasn't deployed yet — needs CRM_USERS env var before auth goes live
- **Waiting on Chris** for William + Jack login credentials
- Express 5 gotcha: `res.sendFile()` was broken, used `fs.readFileSync` + `res.type('html').send()` instead

## Model Routing Issue
- Chris noticed agents defaulting to Claude instead of ChatGPT Codex
- William investigated: default IS gpt-5.4, Claude is fallback
- Agents fall to Claude when Codex times out or fails — infra issue, not config
- William offered to restart gateway
