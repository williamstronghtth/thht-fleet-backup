
## Inter-Agent Timing Fix
- **Problem:** William's content handoff (11:00 UTC) had no matching Fiona daily cron to pick it up
- **Fix:** Added Fiona daily cron at 11:30 UTC — 30min buffer after William's handoff
- **Also:** Updated Fiona's TOOLS.md to check both inbox/ and inbox/processed/
- **Also:** Added email/MCP warning to all 12 agents' TOOLS.md — use /root/agents/bin/send-email.py, never ask Chris for Gmail auth

## Pre-Commit Quality Gate Hook
- **Script:** `/root/agents/bin/pre-commit-gate.sh`
- **Claude Code hook:** `/root/agents/bin/hooks/pre-commit-check.sh` (PreToolUse for Bash)
- **Installed:** Symlinked as pre-commit hook in all 24 repos across agent workspaces
- **Blocks:**
  - Secrets (AWS keys, API keys, JWTs, private keys, hardcoded passwords, .env files)
  - `console.log/debug/warn/error/info()` in JS/TS files
  - `debugger` statements in JS/TS files
  - `.env` files
- **Bypasses:** `// keep` comment on console.log lines, `git commit --no-verify` for emergencies
- **Bug fixed:** `grep -v '^\+\+\+'` needed `-vE` flag for consistent behavior
- **Note:** settings.json at `/root/.claude/settings.json` needs write permission to enable Claude Code hook

## Post-Edit Auto-Format Hook
- **Script:** `/root/agents/bin/hooks/post-edit-format.sh`
- **Trigger:** PostToolUse on Write|Edit tools (via `/root/.claude/settings.json`)
- **Formats:** JS, JSX, TS, TSX, CSS, SCSS, LESS, HTML, JSON, MD, YAML, GraphQL, Vue, Svelte
- **Skips:** node_modules, dist, build, .git, vendor, .next, and non-supported file types
- **Engine:** Prettier 3.8.2 (via npx), uses project .prettierrc if present
- **Safety:** Always exits 0 — formatting failure never blocks edits
- **Also installed:** PreToolUse hook for pre-commit quality gate (was previously blocked)

## Session Summary on Stop Hook
- **Hook type:** `Stop` (prompt-based) in `/root/.claude/settings.json`
- **Trigger:** Fires when any agent is about to stop
- **Action:** Injects a prompt telling the LLM to append a session summary to its memory file
- **Summary includes:** timestamp, actions taken, key decisions, open items, files changed
- **Format:** 10-15 lines max, appended to `/root/agents/<agent-id>/workspace/memory/<date>.md`
- **Memory dirs:** Created for all 12 agents

## Session — 14:46 ET
- **What was done:** Confirmed Session End Protocol is visible and functioning in system prompt
- **Key decisions:** None — quick check-in session
- **Open items:** None from this session
- **Files changed:** memory/2026-04-10.md (this entry)

## Session End Protocol — Fixed
- **Problem:** `Stop` hook in settings.json doesn't fire in `claude -p` (pipe) mode
- **Fix:** Added "Session End Protocol" section to system prompt built by `run-agent.sh`
- **Location:** `/root/agents/bin/run-agent.sh` — injected before Inter-Agent Messaging section
- **Tested:** ✅ Agent ran, saw the protocol, wrote summary to memory file in correct format
- **Belt & suspenders:** Kept the `Stop` hook in settings.json too (covers interactive sessions)

## Session — 14:49 ET
- **What was done:** User typed "config" — presented Claude Code configuration options (permissions, hooks, status line, keybindings)
- **Key decisions:** None — user hadn't selected an option yet
- **Open items:** User's config intent unknown — session ended before selection
- **Files changed:** memory/2026-04-10.md (this entry)

## Global Coding Rules Installed
- **Location:** `/root/.claude/rules/` (3 files, 107 lines total)
- **code-quality.md** — 800-line file max, 50-line function max, immutability (`const` > `let`, no `var`), naming conventions (verb-first functions, boolean prefixes, UPPER_SNAKE constants, kebab-case files), structure (import ordering), error handling (no silent swallowing), comment standards (why not what)
- **security.md** — No hardcoded secrets, parameterized queries, input validation, dependency hygiene (pin versions, justify additions), auth server-side, no PII in logs, CORS/XSS rules
- **patterns.md** — Stack conventions (Express 5, vanilla JS, Supabase, Render), API design (RESTful, consistent JSON shape, proper status codes), async/await, CSS variables + BEM, semantic HTML, git commit style, test naming
- **How it works:** Claude Code auto-loads all `.md` files from `~/.claude/rules/` into every session globally — every agent gets these standards injected automatically

## Session — 18:52 ET
- **What was done:** Checked whether `rules/common/` files (coding-style.md, security.md, git-workflow.md) were created per part 2 plan. Found they were NOT — only `~/.claude/rules/` global files exist (code-quality.md, security.md, patterns.md), which cover Claude Code sessions but aren't in the agents infra.
- **Key decisions:** Presented two options to user (skip since ~/.claude/rules covers it, or create rules/common/ for belt-and-suspenders). User hasn't chosen yet.
- **Open items:** `rules/common/` creation pending user decision. Also got Stop hook feedback — previous session failed to write summary. Fixed by writing this one.
- **Files changed:** memory/2026-04-10.md (this entry)

## Model Routing by Task Complexity
- **Config:** `/root/agents/config/model-routing.json`
- **Router:** `/root/agents/bin/resolve-model.py` — reads config, matches task message against regex patterns
- **Integration:** `run-agent.sh` now calls resolve-model.py before launching claude, passes `--model` flag
- **New CLI flag:** `--model haiku|sonnet|opus` overrides auto-routing
- **Routing tiers:**
  - **Haiku** (cheap): keep-alives, digests, briefings, work pulses, content handoffs, inbox processing, domain warmup, story delivery
  - **Sonnet** (default): SEO writing, cadence runners, betting analysis, most agent work
  - **Opus** (expensive): Ryan default (coding), architecture/refactor/design tasks, code review/debugging
- **Priority:** upgrade patterns → downgrade patterns → agent default → fallback (sonnet)
- **Tested:** All 15 crontab tasks route correctly

## Session — 14:55 ET
- **What was done:** Built model routing system — config file, Python resolver, run-agent.sh integration
- **Key decisions:** Three-tier routing (haiku/sonnet/opus); pattern matching on task message; upgrade patterns checked before downgrades so architecture tasks always get Opus
- **Open items:** Monitor token savings over next week; may need to tune patterns as new tasks are added
- **Files changed:** `/root/agents/config/model-routing.json` (new), `/root/agents/bin/resolve-model.py` (new), `/root/agents/bin/run-agent.sh` (modified), `memory/2026-04-10.md`

## Session — 15:05 ET
- **What was done:**
  - Built De-Sloppify Pattern — separates implementation from review into two focused passes
  - Created `/root/agents/bin/cleanup-pass.sh` — post-run reviewer using Haiku (cheap, fast)
  - Created `/root/agents/config/cleanup-checklist.md` — positive-framed quality standards
  - Added `--cleanup` and `--cleanup-fix` flags to `run-agent.sh`
  - Rewrote negative instructions in AGENTS.md across all 12 agents:
    - "Don't exfiltrate" → "Keep private data private"
    - "Don't respond to every message" → "Let silence be your default"
    - "Avoid the triple-tap" → "One thoughtful response beats three fragments"
    - Removed "Don't overdo it", "Don't dominate" phrasing
  - Preserved safety guardrails (reframed positively, not removed)
- **Key decisions:**
  - Cleanup runs on Haiku to keep costs low — it's a reviewer, not an implementer
  - Two modes: `--cleanup` (report only) and `--cleanup-fix` (auto-fix + report)
  - Safety instructions stay in AGENTS.md (reframed positive) — they're guardrails, not quality constraints
  - Quality/style checking moves to the cleanup pass
- **Open items:**
  - Consider adding `--cleanup` to specific cron tasks (implementation-heavy ones like Derek's SEO, Ryan's CRM health checks)
  - Monitor cleanup pass token usage vs quality improvement
- **Files changed:**
  - `/root/agents/bin/cleanup-pass.sh` (new)
  - `/root/agents/config/cleanup-checklist.md` (new)
  - `/root/agents/bin/run-agent.sh` (modified — new flags)
  - All 12 agents' `AGENTS.md` (rewritten negative → positive)

## Session — 15:22 ET
- **What was done:** Built iterative context retrieval for subagent calls (step 5 of improvement plan)
  - Created `bin/context-resolver.py` — classifies tasks into tiers (0-4), resolves minimum context needed
  - Updated `run-agent.sh` — uses context-resolver for system prompt building, retry loop on missing context
  - Added `--full-context` and `--tier N` flags for manual override
  - Tier 0 (ping): ~1KB vs Tier 4 (full): ~27KB — up to 96% reduction for simple tasks
- **Key decisions:**
  - 5 tiers: Core → Memory → Role → History → Full (cumulative)
  - File triggers (keyword → force-include specific files) for cross-tier needs
  - Max 1 retry: if agent signals missing context, escalate to tier 4 and re-run
  - Unknown/unclassified tasks default to tier 4 (safe fallback)
- **Open items:** Monitor real-world tier distribution; may need to tune patterns
- **Files changed:** `bin/context-resolver.py` (new), `bin/run-agent.sh` (modified)

## Session — 15:43 ET
- **What was done:** Recapped the Top 5 Optimizations from ECC plan — confirmed all 5 are complete, no step 6 needed
- **Key decisions:** Plan is done at 5 steps (not 6); de-sloppify counted as a bonus, not a numbered step
- **Open items:** User asked about tuning or wrapping up infra — no decision made yet
- **Files changed:** memory/2026-04-10.md (this entry)

## Session — 16:01 ET
- **What was done:** Updated Gmail app password for Jack Sullivan's SMTP access across 21 files (1 .env + 20 scripts). Tested SMTP connection — still getting 535 BadCredentials from Google, same as before.
- **Key decisions:** Password is correct but Google account-level issue persists. Identified likely causes: Workspace admin policy blocking SMTP, app password possibly generated on wrong account, or propagation delay after 2FA enable.
- **Open items:** Chris needs to verify: (1) app password was created under jack@ not another account, (2) Google Workspace admin console allows SMTP/app passwords, (3) consider smtp-relay.gmail.com as alternative. Also noted all 20 of Jack's scripts hardcode passwords — should migrate to env vars eventually.
- **Files changed:** /root/agents/.env (updated password), 20 files in jack-sullivan/workspace/ (scripts, campaigns, newsletter), memory/2026-04-10.md

## Session — 16:12 ET
- **What was done:** Tested new Gmail app password (xhqd injc pxms vuxi) for jack@thehooverhometeam.com SMTP. Still getting 535 BadCredentials — tried port 587 (TLS), port 465 (SSL), with/without spaces. All fail. Confirmed .env and all 20 scripts already have the new password from the earlier session. Sent Chris a Telegram with 3 specific things to check on the Google Workspace admin side.
- **Key decisions:** This is definitively a Google account-level issue, not a code/password issue. The three most likely causes: (1) app password created under wrong Google account (admin vs jack@), (2) Workspace admin policy blocking SMTP/app passwords, (3) app password needs to be regenerated directly under jack@'s account.
- **Open items:** Waiting on Chris to verify Google Workspace settings and potentially regenerate the app password under jack@'s account directly. Code side is ready — just needs a working password.
- **Files changed:** memory/2026-04-10.md (this entry)

## Session — 16:11 ET
- **What was done:** Tested new Gmail app password for Jack Sullivan SMTP — same 535 BadCredentials failure on both port 587 (TLS) and 465 (SSL). Password is identical to what's already in all 21 files. Confirmed this is a Google account-level issue, not a password mismatch.
- **Key decisions:** No file changes needed (password unchanged). Identified 3 action items for Chris: verify app password was created under jack@ account, check Workspace admin SMTP policies, consider SMTP relay as alternative.
- **Open items:** Waiting on Chris to verify Google Workspace settings. SMTP won't work until the account-level auth issue is resolved.
- **Files changed:** memory/2026-04-10.md (this entry)

## Session — 16:21 ET
- **What was done:** Updated Jack's Gmail app password (hwxg mdeq ocue spri) across all 21 files. Tested SMTP — **SUCCESS!** Login works on port 587 TLS. Notified Chris via Telegram.
- **Key decisions:** Third app password was the charm — previous two failed due to Google Workspace config. This one works.
- **Open items:** Jack's scripts still hardcode passwords instead of using env vars — should migrate eventually.
- **Files changed:** 21 files (1 .env + 20 scripts/campaigns), memory/2026-04-10.md

## Session — 16:22 ET
- **What was done:** Tested new app password for jack@thehooverhometeam.com — SMTP login SUCCESS on port 587. Sent test email successfully. All 12 files (8 scripts + 4 campaign files + .env) already had the correct password from earlier session. Notified Chris via Telegram.
- **Key decisions:** No file changes needed — password was already updated. The previous 535 BadCredentials was a Google account-level issue that Chris resolved.
- **Open items:** Jack's email campaigns are fully operational. No blockers remaining.
- **Files changed:** memory/2026-04-10.md (this entry)
