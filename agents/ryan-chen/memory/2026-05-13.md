
## Session — 12:31 ET
- **What was done:** Fiona reported Claude API 400 error "Could not process image" when trying to read 5 Osprey Lane PDF listing. Investigated the PDF (3-page MLS listing, 1.3MB, valid). The error is from the Anthropic API failing to process the PDF-to-image conversion on her agent's side. Extracted all listing data manually and saved to her inbox as `5-osprey-lane-extracted.md` so she's not blocked.
- **Key decisions:** Extracted data for Fiona rather than trying to fix the API issue (which is a platform-level problem)
- **Open items:** None
- **Files changed:** /root/agents/fiona-murphy/workspace/inbox/5-osprey-lane-extracted.md (created)

## Session — 16:29 ET
- **What was done:** Fixed "Argument list too long" error in `/root/agents/bin/run-agent.sh` affecting Oliver (and potentially all agents with large system prompts). The system prompt was being passed as a CLI argument (`--system-prompt "$BUILT_PROMPT"`), which exceeded the OS arg limit. Changed both instances (lines 259 and 274) to use `--system-prompt-file "$TMPFILE"` instead — the prompt was already being written to `$TMPFILE` on line 245, so this was a clean fix.
- **Key decisions:** Used `--system-prompt-file` flag (confirmed it exists via `--bare` help text and live test)
- **Open items:** None — Oliver should work on next message
- **Files changed:** /root/agents/bin/run-agent.sh (2 lines changed)

## Session — 17:28 ET
- **What was done:** Acknowledged thanks from William/Chris re: the run-agent.sh fix
- **Key decisions:** None
- **Open items:** None
- **Files changed:** memory/2026-05-13.md
