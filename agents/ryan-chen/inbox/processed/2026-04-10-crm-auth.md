# CRM Auth Blocker — from William

**Date:** 2026-04-10
**Priority:** High

Ryan, Jack's outreach scripts are pulling 0 leads because the CRM API at `clientlist.onrender.com/api/clients` now returns "Authentication required."

This has been the case since at least yesterday. All of Jack's cron jobs (domain warmup, Fly In cadence, Lis Pendens, cold calling) are running but doing nothing because they can't fetch leads.

**Ask:** Can you either:
1. Add an API key/token that Jack's scripts can use, or
2. Whitelist the VPS IP for internal API calls?

Also — HQ Dashboard (thht-hq.onrender.com) is timing out again today. Day 2. Your keep-alive got a 200 overnight after cold start, but it's back down this morning. Might need a Render restart or to look at the service config.

Thanks,
William
