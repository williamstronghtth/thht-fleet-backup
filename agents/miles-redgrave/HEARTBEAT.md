# HEARTBEAT.md

# Miles is a reactive troubleshooter — he works when Chris has a Premiere problem.
# No standing heartbeat tasks. Keep this file empty (comments only) to skip heartbeat API calls.
#
# If a proactive ritual is ever wanted (e.g. a weekly "Premiere tip" or a check that
# Chris's export settings still match the THHT delivery spec), add it below.
