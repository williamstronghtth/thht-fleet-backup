# Memory — 2026-09-02

## Session — 07:00 ET
- **What was done:** 
  - Created daily content brief for Fiona (Mont Vernon Labor Day angle, Nashua back-to-school surge, statewide inventory/rates)
  - Brief is text-first, no image dependency, ready for immediate social/email use
  - Delivered to `/root/agents/fiona-murphy/workspace/inbox/daily-content-2026-09-02.md`
  
- **Key decisions:** 
  - Positioned around Labor Day market shift (families thinking about relocation after long weekend)
  - Emphasized back-to-school urgency in Nashua (school starts Sept 4)
  - Statewide angle: inventory surge + rate expectations
  
- **Open items:** 
  - Awaiting PMMS data confirmation (Sept 2 afternoon) to adjust inventory estimates if needed
  - Fiona to confirm image requirements once she reviews brief
  - Chris to provide Fed rate expectations if specific narrative needed
  
- **Files changed:** 
  - Created: `/root/agents/fiona-murphy/workspace/inbox/daily-content-2026-09-02.md`

## Session — 09:25 ET

- **What was done:**
  - Fiona had rejected my 07:00 content brief (3 uncleared figures). She was right. Unblocked her with a full replacement.
  - **Wrote CLEARED-FIGURES-2026-09-02.md** — it did not exist at 08:40. Fourth late occurrence (Aug 28, Aug 29–30, Sept 1, Sept 2). Carry-forward from Sept 1; figures unchanged (PMMS is Thursday, not today — my own memory file had this wrong).
  - **Found the real defect:** `fair_housing.py` scanned the bad brief and returned **ZERO findings**. Iris found 8 violations by hand. The gate was correctly wired and pointed at the right file — Iris's routing diagnosis was wrong; the failure was **coverage**.
  - Root cause: every FH rule tested for an *evaluative word* ("top-rated", "perfect for"). The brief steered using **bare demographic nouns as sentence subjects** ("Families who spent the long weekend…"). The steer rides on the noun, not the adjective.
  - Added 6 rules: demographic-as-subject, demographic-as-target-audience, school-proximity-as-amenity, school-calendar-as-deadline, schools-as-marketing-bullet, resident-character words ("community feel", "belonging"). Suite **59 → 79 cases, all passing**; 10 new failure cases verbatim from real copy, 10 new honest sentences to hold the false-positive floor.
  - Re-scan of withdrawn brief: **0 → 14 findings**. Fiona's queued Mont Vernon draft: 4 blockers — she pulled it to `drafts/withdrawn/`.
  - Wrote `daily-content-2026-09-02-REVISED.md`: Angle 1 rebuilt on Mont Vernon 47 DOM vs county 24 (no demographics), Angle 2 on the 7-year inventory high with the "still tight" ban, Angle 3 rates with a hard Thursday-noon expiry. **brief-gate: CLEAN on figures and Fair Housing.**
  - **Installed the 06:00 cleared-figures stub cron** (`cleared-figures-stub.py`) — verified via `crontab -l`.
  - Re-ran back-catalog audit under new rules: **40 editable blockers / 17 files**, **49 delivered blockers / 15 sent newsletters** (May 12 → Aug 18, unrecallable). Last week's audit found 19.
  - Telegrammed Chris; replied to Iris correcting her routing diagnosis.

- **Key decisions:**
  - Overrode Fiona's "Angle 1 is fully clean" — it was clean on *figures* and the worst FH offender in the document. Withdrew the entire brief rather than run any part of it.
  - Withdrew "move before school opens" on **two** independent grounds (uncleared figure + familial-status steering) so it can't come back through either door.
  - Did **not** use the gate's quarantine marker to hide my own restated bad figures — rewrote the prose instead. Quarantining my own text to pass my own gate is the walk-around pattern this whole system exists to stop.

- **Lessons (both now amendments in the block):**
  - **A control that runs and reports clean is indistinguishable from one that isn't running — unless a human reads the artifact.** Two consecutive days a hand-read beat a freshly-passing suite. The suite is the floor, not the backstop.
  - **A control is not shipped until its scheduler line exists.** Sept 1 wrote an amendment *stating* a 06:00 cron existed. Only the prose shipped. Verify with `crontab -l`, never by memory.
  - **A permitted fact becomes a prohibited verdict the moment it is offered as a reason to prefer the town.** ("5-minute school commute" passed because distance is on the permitted list.)

- **Open items:**
  - ⚠️ **Chris decision needed:** 49 FH blockers in 15 already-sent newsletters — disclose or record-and-move-on?
  - ⚠️ **Chris decision, 7 days pending:** stale ~$520K Nashua figure still public; Iris has a correction drafted. Recommend running it.
  - 40 editable back-catalog blockers — I fix these next.
  - Town news digests are ungated and unsourced (Iris guardrail #1: a town fact is a figure). Adopted in principle, not yet enforced.
  - Owe Iris a real answer on the `AUDIENCE: INBOUND | LOCAL` Spark.
  - Confirm which angle(s) Fiona actually runs today.

- **Files changed:** `CLEARED-FIGURES-2026-09-02.md` (new), `scripts/fair_housing.py`, `scripts/test-fair-housing.py`, `scripts/cleared-figures-stub.py` (new), crontab, `fiona-murphy/.../daily-content-2026-09-02-REVISED.md` (new), `iris-vale/.../2026-09-02-william-reply-content-brief.md` (new)

## Session — 12:55 ET

- **What was done:**
  - Cleared the **40 editable back-catalog blockers → 0**. 33 lines rewritten across 18 of Fiona's drafts. Backup at `backups/drafts-pre-fh-remediation-2026-09-02/`; every edit exact-match, hand-authored, with per-line rationale in `scripts/remediate-backcatalog-2026-09-02.py`. Notified Fiona with the full account.
  - **Corrected the number I gave Chris yesterday: 49 → 43 delivered blockers.** Nine were the string "strong" harvested from `<strong>` HTML tags — `[^.!?\n]{0,30}` spans crossed tags freely. Added `_neutralize_markup()`: inline tags → space, block tags → " . " (a boundary no rule may span, so `<td>Strong</td><td>Schools</td>` can't fabricate a hit).
  - **Second scanner defect: `scan()` used `pattern.search`, one hit per rule per line.** A newsletter paragraph is ONE line of HTML, so repeats were silently dropped. Switched to `finditer`. Net 49→43, but some files went UP (07-21: 4→5). Wrong in both directions before.
  - **Biggest find: `.html` was never in the audit's SUFFIXES.** The drafts tree holds **35 .html files — more than its 34 .md files**, several named like WordPress post IDs (49426.html) = live posts. Never opened by any control. They hold **35 blockers** (21 school verdicts, 7 demographic-as-subject, 3 district bullets). Escalated to Chris — fixing local files ≠ fixing the live site; needs a WordPress push, so not doing it unattended.
  - **Third consecutive day a hand-read beat the suite.** Found "Schools are still excellent" live: every school rule required the adjective BEFORE the noun; predicate order walked past all of them. Also "(For Families)" as a bare heading label. Both rules added. Suite 79 → **98 cases, all passing**.
  - Added a WITHDRAWN bucket to the audit — 4 of the "40 fixable" were in `drafts/withdrawn/`, killed that morning. Counting pulled copy as outstanding work overstates live exposure.

- **Key decisions:**
  - **Corrected my own number to Chris unprompted.** An overstated exposure figure burns the gate's credibility as fast as an understated one, and he was making a disclosure decision on it.
  - Cut verdicts, never softened them — "excellent schools"→"strong schools" is a rewording, not a remedy. Replaced with checkable town facts (lot size, commute, NH tax structure).
  - **Touched zero figures.** The $514K Nashua median stays until Chris rules. Editing a number under cover of a language fix is how a figure changes with nobody reviewing it.
  - Removed the SEO query "best small towns in southern NH for families" — we don't chase a keyword we can't lawfully write copy for.

- **Lessons:**
  - **A gate that reports a number nobody has verified is a rumour with a decimal point.** Two independent scanner defects survived because the count looked plausible.
  - **An audit that silently skips a file type reports "clean" for content it never opened** — identical output to a working audit. Derive scanned extensions from a listing of what's THERE, not a guess at what we author in.
  - **Rules that pin word order only cover the phrasings their author pictured.** Sept 2 AM: steer rides on the noun, not the adjective. Sept 2 PM: it also rides on predicate order.
  - **My own edit broke a sentence and only a re-read caught it** — deleting "your kid is not a number" orphaned "People here talk about that a lot." The checker sees banned phrases, never broken referents. Always re-read prose after remediating it.

- **Open items:**
  - ⚠️ **Chris: 35 blockers in ~18 live HTML posts** — offered to prep a reviewable diff. Needs a WordPress push either way.
  - ⚠️ **Chris, 8 days pending:** stale ~$520K Nashua figure still public.
  - ⚠️ **Chris:** 43 delivered blockers / 15 sent newsletters — disclose or record?
  - Town news digests still ungated/unsourced.
  - Still owe Iris an answer on the `AUDIENCE: INBOUND | LOCAL` Spark.
  - Never confirmed which angle Fiona ran today.

- **Files changed:** `scripts/fair_housing.py` (markup neutralization, finditer, 2 new rules), `scripts/test-fair-housing.py` (79→98 cases), `scripts/backcatalog-audit.py` (.html + WITHDRAWN bucket), `scripts/remediate-backcatalog-2026-09-02.py` (new), 18 files under `fiona-murphy/workspace/drafts/`, backup dir (new), `fiona-murphy/workspace/inbox/2026-09-02-william-fh-backcatalog-remediation.md` (new)

## Session — 09:15 ET (Chris: "give Fiona what she needs for today's blog posts")

- **What was done:**
  - Found Fiona was **already unblocked and didn't know it.** She rejected my 07:00 brief, I replaced it with the REVISED brief at 08:42, and she wrote the full blog + both social posts at 08:46. But her six image-logging sessions (08:40–09:05, as Chris sent file_221–226) each carried forward a stale open item: *"awaiting William's response before publishing anything today."* She was sitting on finished, clean copy. **What she needed was not a brief — it was an explicit close of that open item.**
  - **Found a live fail-open in my own `fair_housing.py`.** I scanned her blog with `python3 fair_housing.py <file>` → exit 0, no output. Ran a **positive control** ("perfect for families... top-rated schools") → *also* exit 0, silent. The module **has no `__main__`**; invoking it on a file defines functions and quits. Every direct CLI scan since Aug 31 has been meaningless.
  - Added a `__main__` + `self_test()` positive control that runs on **every** CLI invocation and refuses to report on real copy if the ruleset isn't firing. No args → exit 2 (not 0). Unreadable file → exit 2. Blockers → 1. Clean runs now print **the count of checkable lines scanned**, so "0 findings" and "read nothing" can never look alike again. Suite still 98/98.
  - Verified blast radius: **nothing** shells out to `fair_housing.py` — brief-gate, backcatalog-audit and the test suite all `import` it and call `scan()` correctly. The fail-open was only ever reachable by a human at the shell. That human was me, twice today.
  - Re-scanned her content properly: **70 checkable lines, Fair Housing clean.** Hand-verified all 5 figures in the blog against the cleared block (2,992/+16%/7-yr high, 1,494/+8%, 1.71 months, 4–6 as the definition of balance, Greenwald quote). No "still tight", no "not a buyer's market yet", inventory framed as expanding. Genuinely clean.
  - Wrote her a clearance memo (gated it too), and called her to confirm image, publish, and the 7:30 PM rates post.

- **Key decisions:**
  - **Corrected her flag #1, which would have caused a needless hold.** She flagged the Thu Sept 3 noon expiry as hitting the inventory blog. It doesn't — that's a **PMMS/rate** expiry, and her blog carries no rate. NHAR/Redfin July data doesn't refresh Thursday. Angle 3 (rates) is the only thing on that clock.
  - **Flagged the featured image without overriding.** She picked file_221 (home office) at 08:46 from a pool of one interior, then logged five more images through 09:05. For a post about how many homes are on the market, a whole-property shot fits better. Her call — image selection is hers, and she had a real feed-level reason.

## Session — 15:00 ET (Work pulse 2pm check)

- **What was done:**
  - Verified all current work status and cron scheduling
  - Confirmed Fiona's morning content (blog + 2 social posts) cleared, staged, awaiting 7:30 PM publish gate
  - Checked cron logs and agent inboxes — no new blockers, no execution failures
  - All Fair Housing gates operational: 98 test cases passing, backcatalog fully remediated

- **Key decisions:**
  - All systems nominal — no action needed. Next cron at 7:30 PM ET (publish gate).
  - Waiting on Chris for 5 pending decisions (CRM rotation priority #1).

- **Open items:**
  - 🔨 Town-fact sourcing gate still pending Chris approval to wire
  - 7:30 PM ET: publish-gate.py execution (final pre-flight before content goes live)
  - Chris: 5 decisions (CRM rotation day 13, Nashua figure day 9, WP HTML posts, newsletters, Maps key)

- **Files changed:** None (status check only)
  - Told her explicitly the back-catalog remediation must **not** hold today's publishing.

- **Lessons:**
  - **A stale open item is indistinguishable from a real blocker to the agent holding it.** Fiona did everything right and still stalled, because the line "awaiting William" propagated forward through six unrelated sessions. When I withdraw a blocker I have to close it *by name* with the agent, not just ship the replacement.
  - **Run the positive control BEFORE trusting a clean scan — every time, not once.** Three days running, a hand-read beat this gate. Today the gate wasn't even executing and looked identical to passing. The Aug 31 amendment said *"a gate with no input alerts instead of exiting clean"* — I wrote that rule and then shipped a checker whose CLI violated it for three days.
  - **A library and a CLI are different products.** `fair_housing.py` was correct as an import and inert as a command. Correctness of the rules said nothing about correctness of the invocation.

- **Open items:**
  - Awaiting Fiona's confirmation: featured image chosen, blog published w/ featured image set, rates post queued for 7:30 PM.
  - ⚠️ Chris: 35 FH blockers in ~18 **live** HTML posts (needs WordPress push).
  - ⚠️ Chris, 8 days: stale ~$520K Nashua figure still public.
  - ⚠️ Chris: 43 delivered blockers / 15 sent newsletters — disclose or record?
  - Town news digests still ungated/unsourced. Still owe Iris the `AUDIENCE: INBOUND | LOCAL` answer.

- **Files changed:** `scripts/fair_housing.py` (`__main__` + `self_test()` positive control), `fiona-murphy/workspace/inbox/2026-09-02-william-blog-clearance.md` (new)

## Session — 09:00 ET (workday start)

⚠️ **Timestamp warning for future-me:** the "07:00 / 09:25 / 12:55 ET" labels on today's
earlier entries are **UTC, not ET**. File mtimes confirm: the "12:55" session was ~08:55 ET.
Real ET clock at this session's start was 09:00. Label sessions from `TZ=America/New_York date`, not from the runtime header.

- **What was done:**
  - **Found the security scanner reporting a number that was 88% its own echo.** `bin/security-scan.py` reported **59 LIVE SOURCE findings**; broke it down by file before quoting it — **34 were the scanner reading its own report.** It printed matched lines verbatim (keys included) into `reports/security-scan-latest.txt`, then re-scanned that file weekly. Linear growth, ~+7 phantom/week, zero code change.
  - Four fixes: `redact()` masks secrets to 6 chars **at capture time** (root cause — the report can no longer contain a key); report path allowlisted; `rx.search` → **`finditer`** (was one hit per rule per line — same defect as yesterday's FH scanner); `.md`/`.txt` split out of "LIVE SOURCE — these run" into a `DOCS/NOTES` bucket (a memory file doesn't run).
  - **True number: 7 executable / 20 docs / 4 archive.** Regenerated report — 0 raw keys on disk in it. Not git-tracked, so never pushed.
  - `.env` was **mode 644** — world-readable, holding CRM admin key + Jack's Gmail app password + Late key. → `600`.
  - **Verified rather than assumed the severity:** all 7 findings are one Google *Maps Embed* key. Compared it against `GOOGLE_SEARCH_API_KEY` — genuinely different (`AIzaSyBFw0…` vs `AIzaSyCMEB…`). Embed keys ship in page HTML by design → this is a console referrer restriction, not a rotation fire. **My first comparison returned a false "different keys" because `.env` uses `export ` prefixes and my grep anchored `^GOOGLE_`; re-ran it correctly before reporting.**
  - Confirmed **`sk_` CRM literals are gone from all executable code** — Ryan's half of the Aug 30 ticket did land. Remaining `sk_` hits are inert prose only.
  - Dispatched: Ryan (7 files + scanner correction), Fiona (4 files + angle-confirm ask), Iris (full reply to "The Half We Throw Away"), Chris (Telegram: 1 console action + 4 pending decisions).

- **Key decisions:**
  - **Told Ryan and Fiona my own tool had been lying to them.** I've held Fiona's drafts to gate output for two days; she's owed the knowledge that the same scrutiny found my instruments wrong three days running.
  - **Downgraded the Maps key from "fire" to "tidy-up" in writing** — overstating severity burns the escalation channel as fast as understating it, and Ryan would have stopped a real task for it.
  - **Answered Iris YES on the `AUDIENCE: INBOUND | LOCAL` tag** and made her Spark today's priority — but named the blocker: digests are the *only* content class with no sourcing gate, so publishing the town half puts our least-verified content in front of the readers most able to catch an error. The gate ships the Spark; it isn't an objection to it.

- **Lessons:**
  - **Never quote a scanner's total — quote its breakdown.** Fourth consecutive automated number wrong on first inspection (Sept 1 phantom cron, Sept 2 AM FH 0-findings, Sept 2 AM `<strong>`/`search` miscount, now this). Not one was caught by a control failing loudly. All four by a hand breakdown.
  - **NEW: a reporting tool must never write the thing it detects.** A leak scanner that prints keys is itself a leak — and if it scans its own output, it manufactures its own workload.
  - **A grep that returns empty is not a negative result.** My `^GOOGLE_SEARCH_API_KEY` miss would have shipped a confident, wrong "different keys" claim. Prove the pattern matches *something* before trusting that it matched nothing.

- **Open items:**
  - 🔨 **Today's build: town-fact sourcing gate** (source+date required, events expire, unsourced = blocked, FH applies harder). Unblocks Iris.
  - ⚠️ Chris: Maps key referrer restriction (console, 2 min); Nashua $520K stale figure (**day 9**); 35 FH blockers in live WP posts; 43 in sent newsletters; CRM rotation (day 13).
  - Fiona: which angle actually ran — asked, not yet answered.
  - Iris: asked her what makes local content worth a *resident's* second read. Gate handles correctness, not quality.

- **Files changed:** `bin/security-scan.py` (redact + allowlist + finditer + DOCS bucket), `reports/security-scan-latest.txt` (regenerated, redacted), `/root/agents/.env` (chmod 600), `day-plan-2026-09-02.md` (new), inbox notes → ryan-chen, fiona-murphy, iris-vale (3 new)

## Session — 19:00 ET (Workday end)

- **What was done:**
  - Rolled up complete day summary: shipped (Fiona content, back-catalog remediation, FH gate hardening, security audit), open decisions (5 for Chris), next moves (7:30 PM publish gate, tomorrow's sourcing gate)
  - Composed WORKDAY-SUMMARY-2026-09-02.md for Chris: ✅ shipped, ⚠️ decisions, 📅 next, 🔍 learnings
  - Key theme: four automated counts wrong on first hand-check (Sept 1 cron, Sept 2 AM FH, Sept 2 AM security, Sept 2 CLI)

- **Key decisions:**
  - Prioritized Chris's clarity: lead with shipped work, then name each decision explicitly with context, then next moves
  - Downgraded Maps key again in writing (tidy-up, not fire) — overcommunicated earlier today
  - Noted that FH gate + security scanner both require positive controls to catch fail-opens

- **Open items:**
  - Awaiting Chris on 5 decisions (WP posts, newsletters, Nashua figure, Maps key, CRM rotation)
  - 7:30 PM publish gate execution
  - Town-fact sourcing gate pending approval

- **Files changed:** `william-strong/.../inbox/WORKDAY-SUMMARY-2026-09-02.md` (new)
