
## Session — 08:41 ET
- **What was done:**
  - Created `model/production/run_mc_pregame.py` — pre-game MC runner script
  - Script checks MLB API every 30 min for games 45–85 min from first pitch
  - Only fires when both lineups are confirmed posted (9+ batters each side)
  - Sentinel files in `daily_picks/mc_fired/` prevent double-firing per game
  - Runs 10,000 MC sims via `build_game_from_api()` → formats win%, runs, run line, F5, edges → sends to Chris via Telegram
  - Also warns Chris if a game is imminent but lineups aren't posted yet
  - Saves per-game JSON results to `daily_picks/mc_pregame_{date}_{game_pk}.json`
  - Added 2 system cron entries (persistent, survives restarts):
    - `*/30 15-23 * * *` — day window (11am–7:30pm ET)
    - `*/30 0-5 * * *` — night window (8pm–1am ET)
- **Key decisions:** Used sentinel files to prevent double-fire; 45–85 min window centered on T-60; used system crontab (not session-only) for durability
- **Open items:** None — cron is live and persistent
- **Files changed:** `model/production/run_mc_pregame.py` (new), system crontab (appended)

## Session — 09:00 ET
- **What was done:** Simplified `run_mc_pregame.py` per Chris's feedback — lineups are always confirmed at T-60, so removed the lineup check, lineup-not-posted warning, and the `lineups_locked()` function entirely
- **Key decisions:** Tightened fire window from 45–85 min to 55–75 min (centered on T-60); cleaner and avoids edge cases with very early fires
- **Open items:** None
- **Files changed:** `model/production/run_mc_pregame.py`

## Session — 13:22 ET
- **What was done:** Diagnosed why 6 games missed MC pre-game. Root cause: cron was invoking `bin/run-agent.sh nolan-price` (full Claude agent shell) instead of running the python script directly. The agent layer was silently failing — cron journal confirmed 5 fires (15:00-17:00 UTC), but zero log entries in nolan-price log, meaning run-agent.sh never completed. Fixed by updating crontab to run `python3 run_mc_pregame.py` directly. Also widened fire window from 55–75 min to 45–80 min (was only 20-min window against 30-min cron — too narrow). Sent Chris Telegram explaining root cause + fix. Remaining games today (KC@ATH, SF@PHI eve, TOR@MIN) will be caught by the updated cron.
- **Key decisions:** Cut out the agent layer entirely — script is standalone, handles Telegram itself. Wider window (35 min vs 20 min) guarantees 30-min cron always catches at least one fire per game.
- **Open items:** Verify KC@ATH fires at ~2:00 PM ET cron run
- **Files changed:** `model/production/run_mc_pregame.py` (WINDOW_MIN/MAX), system crontab (direct python3 invocation)

## Session — 13:31 ET
- **What was done:** Received positive confirmation from Chris on the cron fix. No new tasks.
- **Key decisions:** N/A
- **Open items:** Monitor KC@ATH MC pre-game fire at ~2:00 PM ET to confirm fix works
- **Files changed:** None
