## Session — 08:20 ET
- **What was done:** Investigated Chris's question — "only 3 games picked yesterday (7/21) but there were more." Traced every pipeline: no nolan-price agent session ran at all on 7/21 (no session log, no memory/2026-07-21.md file exists — gap between 07-20 and 07-22 logs). Confirmed via /root/agents/logs/.
- **Root cause found:** The 3 games Chris got were from the fully-automated 7:45am SP Matchup Scorer cron (TB@TOR UNDER, MIA@HOU RL lean, WSH@COL OVER) — that script ran fine and worked as designed (only fires signals on qualifying gaps). But the 8am Morning Pre-Screen and 11am Full Model Run — the tasks that require a live agent session to pull lineups/umpires and run the full slate through the model — never fired on 7/21. That's the pipeline that normally covers ALL games, not just SP-gap signals. 13 games were on the slate (per mc_pregame files); using the MC v3 tier logic from 7/20 (PRIMARY 60-65%, SECONDARY 53-55%, TRAP 55-60% never-bet, NO-BET <53%), roughly 4 games would've cleared SECONDARY that day (CWS@TEX 53.3%, MIN@CLE 53.2%, WSH@COL 54.0%, ATH@AZ 54.0%) — none hit PRIMARY, and 3 were in the TRAP zone (STL@LAA 58.1%, CIN@SEA 55.7%, SD@ATL 55.9%) — correctly never-bet.
- **Deeper finding:** This isn't new — memory/2026-07-11.md and 2026-07-15.md already flagged "ML v2 actual bets not logged... no ml_v2_picks files after June 27" and "Fix ML v2 logging gaps before H2 starts" as an open item that was never resolved. ml_v2_picks/ml_v2_results directories confirm zero files since 6/27. HEARTBEAT.md still lists "ML v2 is the only active betting model" but recent sessions (7/20) have shifted to an informal MC v3 tier system that was never written into rules.md or HEARTBEAT.md.
- **Key decisions:** Flagged to Chris — this is an infrastructure/scheduling gap (missed agent session), not a model quality issue. Did not attempt to fix the underlying cron/scheduler since it's outside this workspace's visibility (CronList tool only shows session-local jobs, not the system heartbeat scheduler).
- **Open items:** (1) Figure out why the 7/21 agent session never fired — needs Ryan Chen or infra-level check. (2) Formalize MC v3 tier system into model/rules.md — it's currently only living in memory notes. (3) Decide if ML v2 pipeline should be revived/fixed or fully retired in favor of MC v3 — HEARTBEAT.md is out of date either way.
- **Files changed:** memory/2026-07-22.md

## Session — 08:30 ET
- **What was done:** Chris asked to run MC v3 on every game today "like you've been doing in the past." Investigated how MC v3 (run_mc_pregame.py) actually fires — confirmed via `crontab -l`, `/etc/cron.d/*`, and CronList that there is NO actual cron wired up for it anywhere (system or session). It only ever ran because a live agent session manually re-invoked it every ~30 min throughout the day (explains the 15 mc_pregame files from 7/20). This is the same root cause class flagged in the 08:20 note re: the 7/21 gap.
- **Key decisions:** Set up a session CronCreate job (id a76efd22, "7,37 * * * *", recurring, 7-day auto-expire) that runs `run_mc_pregame.py --date 2026-07-22` every 30 min for the rest of today. Script only Telegrams Chris for PRIMARY/SECONDARY tier games in the T-50/90 window; everything else logs silently. Ran a --dry-run first to confirm: 17 games on today's slate, 0 in-window yet (earliest first pitch ~5hrs out), 0 fired — expected at this hour.
- **Open items:** (1) This session cron dies if this Claude session exits and hard-expires in 7 days — NOT a durable fix. Need Ryan Chen to wire MC v3 into the actual infra-level heartbeat scheduler (like the SP scorer / closing-odds-capture crons) so this doesn't depend on a live session every single day. (2) Still open from earlier: formalize MC v3 tier system into model/rules.md (currently only in memory notes).
- **Files changed:** memory/2026-07-22.md

## Session — 17:15 ET
- **What was done:** Chris asked why no MC picks came through today. Diagnosed root cause definitively: the CronCreate job I set up at 08:30 ET ("every 30 min for the rest of today") is **session-scoped and in-memory only** — it died within ~2 min when that morning session exited (confirmed: CronList now shows "No scheduled jobs", and the tool's own docs confirm jobs are gone when the creating session ends). This means my 08:30 "fix" never had a chance to work at all — not a fluke, a structural limitation of CronCreate. Zero mc_pregame_2026-07-22 files existed before this session (confirmed via find). Also re-confirmed no real system cron exists for MC v3 (crontab -l empty, /etc/cron.d has only unrelated jobs).
- **Key decisions:** (1) Wrote a detailed inbox message to Ryan Chen (/root/agents/ryan-chen/workspace/inbox/2026-07-22-mc-v3-cron-not-durable.md) asking him to wire run_mc_pregame.py into the real infra-level heartbeat (same mechanism as sp_matchup_score.py / scrape_closing_odds.py), since session-scoped cron can never provide durable same-day coverage. (2) Ran run_mc_pregame.py --date 2026-07-22 live as an immediate stopgap — first as --dry-run (killed after 1 game, MIN@CLE 824408, which was TRAP zone/correctly suppressed so no harm), then for real in the background. (3) Telegrammed Chris an explanation + status update immediately rather than waiting silently.
- **Open items:** (1) MC v3 real run was still in progress as of end of session — need to verify final Telegram picks went out and file results. (2) Ryan Chen fix is the durable solution; until wired at infra level, MC v3 WILL keep silently failing on any day without manual babysitting. (3) Still open from earlier sessions: formalize MC v3 tier system into model/rules.md; decide fate of retired ML v2 pipeline.
- **Files changed:** memory/2026-07-22.md, /root/agents/ryan-chen/workspace/inbox/2026-07-22-mc-v3-cron-not-durable.md

## Session — 21:22 ET
- **What was done:** Chris asked to "fix this moving forward" re: MC v3 not covering every game ~90min before first pitch. Re-verified root cause is unchanged from earlier today's finding: no real system cron exists for run_mc_pregame.py (checked /etc/cron.d and crontab -l again — only unrelated system jobs like docker-prune/e2scrub/sysstat). Session-scoped CronCreate structurally cannot solve this (dies on session exit). Escalated directly to Ryan Chen via run-agent.sh (not just async inbox note) with a concrete, actionable ask: wire run_mc_pregame.py into the same infra-level heartbeat mechanism that reliably runs sp_matchup_score.py and scrape_closing_odds.py, cadence every 20-30 min from ~11am-1am ET. Asked him for explicit can-do-now/needs-someone-else/blocked status. Call is running in background (timed out at 180s, moved to background task b38d242lc) — awaiting his reply.
- **Key decisions:** Did not re-attempt the session CronCreate stopgap today since it's 21:22 ET and most of today's slate has already started/passed — only 2 of the day's games got MC pregame coverage (823438, 824408). Prioritized getting the durable fix moving over another same-day patch that would just repeat yesterday's failure mode.
- **Open items:** (1) Waiting on Ryan Chen's response — need to relay his status to Chris once it lands. (2) If Ryan confirms he can wire it, verify it actually fires tomorrow (7/23) covering the full slate. (3) Still open: formalize MC v3 tiers into model/rules.md; decide fate of retired ML v2 pipeline.
- **Files changed:** memory/2026-07-22.md (this entry)

## Session — 21:34 ET
- **What was done:** Chris said "let me know when it's fixed" re: MC v3 cron gap. Verified the fix landed: `crontab -l` now shows a real system-level entry — `*/30 15,16,17,18,19,20,21,22,23,0,1,2,3,4,5 * * * /root/agents/bin/nolan-mc-cron.sh` (every 30 min, 11:30am ET–1:30am ET) — this is durable (system crontab, not session-scoped CronCreate). Confirmed it's actually firing correctly: `nolan-price-mc.log` shows a clean 01:30 UTC run (exit=0) covering all 17 games on the 7/22 slate, correctly skipping games already fired earlier in the day. Ryan Chen's session log shows he picked up my 21:22 ET escalation (opus session started 21:22:42) and the crontab spool file was modified 2 min later at 21:24 ET — consistent with him making the fix. Also spotted (in ryan-chen's unrelated weekly review log) a mention that a separate crontab.txt/live-file drift-sync preserved the nolan-mc multi-fire cadence rather than reverting it — good sign this is now the canonical state.
- **Key decisions:** Confirmed via ground truth (crontab -l + actual log output showing real games processed), not just trusting that Ryan said he'd do it. Telegrammed Chris confirmation immediately.
- **Open items:** (1) Watch tomorrow's (7/23) full slate to make sure PRIMARY/SECONDARY tier picks actually Telegram through on the new durable cron — first real end-to-end validation. (2) Still open from earlier sessions: formalize MC v3 tier system into model/rules.md; decide fate of retired ML v2 pipeline.
- **Files changed:** memory/2026-07-22.md (this entry)
