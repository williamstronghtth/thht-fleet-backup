
## Session — 21:34 ET — Slate coverage investigation

**Chris complaint:** "I got two games only today. Why am I not getting the full slate?"

**Diagnosis (three separate things, only one is a real bug):**

1. **Today (7/23) genuinely had a 5-game slate.** MLB Stats API confirms: 5 games
   (Thu getaway day). Not 15. MC v3 simulated all 5 — 100% coverage.
   Of those 5, only 2 cleared the confidence gate and Telegrammed:
   SD@ATL (61.7% primary), KC@DET (61.1% primary).
   Suppressed by design: MIN@CLE 55.6%, TB@TOR 56.2% (55-60% TRAP ZONE, 49.8%
   historical acc), AZ@STL 50.4% (<53% NO ACTION).
   → "Two games" is the model working correctly on a short slate.

2. **REAL BUG — 7/22 cron outage.** `nolan-price-mc.log` has a 20-hour hole:
   last run Wed Jul 22 05:30 UTC, next run Thu Jul 23 01:30 UTC. The entire
   7/22 slate window (15:00-23:00 UTC) never fired. 2 of 17 games simulated.
   Heartbeat log also missing 13:13 + 15:00 UTC runs that day.
   `last` shows a root login Jul 22 12:05-12:11 UTC — matches Ryan's crontab
   rewrite. The crontab install ate that day's runs. Self-healed 7/23.
   → This is why this morning's autoscore said "ML: 0/2" — 2 games, not 15.
     That's almost certainly the message that set Chris off.

3. **run_ml_v2.py is NOT in the pipeline.** run_heartbeat.py:263-264 runs
   run_runline_v2.py with comment "ML v2 removed 2026-06-27 — not used in
   production". But HEARTBEAT.md says RL is RETIRED and ML v2 is the ONLY
   active betting model. No ml_v2_picks_*.json since 2026-06-27.
   → Code and docs directly contradict. NEEDS CHRIS'S CALL, did not touch.

**Fixes shipped:**
- `scrape_odds_api.py` — date filter compared TARGET_DATE against UTC
  commence_time, silently dropping every West Coast night game (10pm ET =
  next-day UTC). Added `_is_on_target_date()` converting UTC→ET first.
  Verified: 7/24 now returns 15/15 incl. the 9:45pm ET LAA@SF.
- `check_slate_coverage.py` (NEW) — watchdog comparing mc_pregame_*.json
  against real MLB slate; alerts <80% coverage. Correctly flags 7/22 as 2/17,
  7/21 and 7/23 as 100%.
- `/root/agents/bin/nolan-coverage-cron.sh` + crontab 13:20 UTC daily.
- Moved Telegram creds out of hardcoded bash into gitignored `.env`
  (NOLAN_TELEGRAM_BOT_TOKEN / NOLAN_TELEGRAM_CHAT_ID).

**Open items:**
- ML v2 vs RL v2 production mismatch — awaiting Chris.
- Tomorrow 7/24 is a 15-game slate. Verify full coverage as the real test.
- Backfill 7/22? Can't — lineups/odds are gone. That day is a data hole.

**Files changed:** model/production/scrape_odds_api.py,
model/production/check_slate_coverage.py (new), /root/agents/bin/nolan-coverage-cron.sh (new),
.env, crontab

## Session — 21:42 ET
- **What was done:** Chris responded to the slate-coverage explanation with "you're only showing games with 60% or more confidence? That works, let's keep that." Verified the actual gate logic in `run_mc_pregame.py` (`_classify_confidence_v3`, lines 146-192, gate at 384-401) before agreeing — it is NOT a flat 60% cutoff. Corrected him with the real two-band structure.
- **Confirmed tier logic (ground truth from code):**
  - `<53%` → NO ACTION, suppressed
  - `53-55%` → SECONDARY, 59.1% acc (63.2% June), fires Telegram at small size
  - `55-60%` → TRAP ZONE, 49.8% acc, never bet, suppressed
  - `60-65%` → PRIMARY, 67.6% acc
  - `65%+` → PRIMARY, 63.3% acc
  - Venue-risk adjustment can demote a PRIMARY to "SECONDARY (venue discount)" if `adjusted < 0.60`.
- **Key decisions:** Did not just accept Chris's "60%+" framing — the 53-55% SECONDARY band also fires, and he'd have been confused the next time a 54% game came through. Explicitly called out the counterintuitive part: 55-60% hits WORSE (49.8%) than the 53-55% band below it, which is why a 56% game gets killed and a 54% game gets sent. Model config unchanged — Chris signed off on keeping it.
- **Open items:** (1) Today 7/24 is a genuine 15-game slate — first real end-to-end test of Ryan's durable system cron + my `scrape_odds_api.py` UTC/ET date-filter fix. Verify full coverage. (2) STILL OPEN, needs Chris: `run_heartbeat.py` runs RL v2 while HEARTBEAT.md says ML v2 is the only active model — code/docs contradiction, no `ml_v2_picks` file since 6/27. (3) STILL OPEN from 7/22: formalize MC v3 tiers into `model/rules.md` — they only live in code + memory notes, which is exactly why this confusion happened.
- **Files changed:** memory/2026-07-23.md (this entry)
